{
    "name": "kinopro",
    "private": true,
    "scripts": {
      "dev": "concurrently \"cd backend/src && npm run dev\" \"cd frontend && npm run dev\"",
      "install:all": "npm install && cd backend/src && npm install && cd ../../frontend && npm install"
    },
    "devDependencies": {
      "concurrently": "^8.0.0"
    }
  }